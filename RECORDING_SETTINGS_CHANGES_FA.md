# تغییرات فنی و عملکردی تنظیمات ذخیره ویدئو

## هدف

این تغییرات برای فراهم‌کردن امکان مدیریت نحوه ذخیره ویدئو توسط کاربران مجاز پیاده‌سازی شده است. تنظیمات ذخیره‌سازی اکنون می‌توانند به‌صورت سراسری یا اختصاصی برای هر دوربین تعریف شوند و روی ضبط پیوسته و ضبط زمان‌بندی‌شده اثر بگذارند.

## تغییرات عملکردی

### تنظیمات قابل انتخاب

کاربر دارای مجوز می‌تواند موارد زیر را تنظیم کند:

- فعال یا غیرفعال‌بودن ذخیره پیوسته ویدئو
- کیفیت خروجی: کم، متوسط، زیاد یا اصلی
- مدت هر قطعه ویدئو: ۱، ۲، ۵ یا ۱۵ دقیقه
- مدت نگهداری در MinIO: ۷، ۳۰، ۹۰ یا ۳۶۵ روز

مقادیر پیش‌فرض عبارت‌اند از:

- ذخیره پیوسته: غیرفعال
- کیفیت: متوسط
- مدت قطعه: ۲ دقیقه
- مدت نگهداری: ۳۰ روز

### مشخصات کیفیت‌ها

| کیفیت | وضوح | نرخ فریم | نرخ بیت |
|---|---:|---:|---:|
| کم | 640×360 | 10 FPS | 0.75 Mbps |
| متوسط | 1280×720 | 15 FPS | 2 Mbps |
| زیاد | 1920×1080 | 25 FPS | 5 Mbps |
| اصلی | وضوح و FPS منبع | منبع | حداکثر 8 Mbps |

### تنظیمات سراسری و اختصاصی دوربین

- تنظیمات سراسری برای تمام دوربین‌هایی استفاده می‌شوند که تنظیم اختصاصی ندارند.
- برای هر دوربین می‌توان یک تنظیم اختصاصی ثبت کرد.
- با بازنشانی تنظیم دوربین، آن دوربین دوباره از تنظیمات سراسری ارث‌بری می‌کند.
- تغییر کیفیت با نهایی‌کردن قطعه جاری و بازسازی امن شاخه ضبط اعمال می‌شود.

### رفتار ضبط پیوسته و تمام‌صفحه

پیش از این، ساخته‌شدن شاخه تمام‌صفحه باعث شروع خودکار ذخیره ویدئو می‌شد. این وابستگی حذف شد:

- مشاهده تمام‌صفحه به‌تنهایی باعث ذخیره ویدئو نمی‌شود.
- ضبط پیوسته تنها زمانی فعال است که سیاست مؤثر دوربین آن را فعال کرده باشد.
- ضبط پیوسته بدون نیاز به بازبودن صفحه مرورگر یا حالت تمام‌صفحه ادامه پیدا می‌کند.
- ضبط زمان‌بندی‌شده همچنان می‌تواند شاخه ضبط را تا پایان کار فعال نگه دارد.

### ضبط زمان‌بندی‌شده

- ضبط زمان‌بندی‌شده کیفیت و مدت نگهداری مؤثر دوربین را به ارث می‌برد.
- برای جلوگیری از مصرف اضافه GPU، کیفیت جداگانه‌ای در سطح هر درخواست زمان‌بندی‌شده انتخاب نمی‌شود.
- مشخصات کیفیت، ابعاد، FPS، نرخ بیت و مدت نگهداری در رکورد کار ضبط ذخیره می‌شوند.

## تغییرات دسترسی و رابط کاربری

یک منبع مجوز جدید با نام `recording_settings` اضافه شد:

- `recording_settings.read`: مشاهده تنظیمات
- `recording_settings.edit`: ویرایش تنظیمات سراسری و دوربین
- `recording_settings.reset`: بازنشانی تنظیمات

در مهاجرت پایگاه داده، کاربرانی که مجوزهای متناظر تنظیمات عمومی را داشتند، مجوزهای اولیه تنظیمات ضبط را نیز دریافت می‌کنند. پس از آن، مدیریت مجوزها از طریق سامانه دسترسی موجود انجام می‌شود.

یک پنل فارسی با عنوان «تنظیمات ذخیره ویدئو» به داشبورد اضافه شد. این پنل:

- تنظیمات سراسری و دوربین انتخاب‌شده را نمایش می‌دهد.
- فقط از شناسه مبهم دوربین استفاده می‌کند و URI دارای اطلاعات حساس را به مرورگر ارسال نمی‌کند.
- در صورت نداشتن مجوز خواندن، مخفی می‌شود.
- امکان ذخیره و بازنشانی تنظیمات را مطابق مجوز کاربر فراهم می‌کند.

## APIهای جدید

پیشوند API:

```text
/api/v1/recording-settings
```

مسیرها:

| متد | مسیر | کاربرد |
|---|---|---|
| `GET` | `/api/v1/recording-settings` | دریافت تنظیمات سراسری، گزینه‌ها و فهرست امن دوربین‌ها |
| `PATCH` | `/api/v1/recording-settings` | ویرایش تنظیمات سراسری |
| `POST` | `/api/v1/recording-settings/reset` | بازنشانی تنظیمات سراسری |
| `GET` | `/api/v1/recording-settings/cameras/{source_ref}` | دریافت تنظیم مؤثر و override دوربین |
| `PATCH` | `/api/v1/recording-settings/cameras/{source_ref}` | ایجاد یا ویرایش تنظیم اختصاصی دوربین |
| `DELETE` | `/api/v1/recording-settings/cameras/{source_ref}` | حذف override و بازگشت به تنظیم سراسری |

پاسخ API شامل سه بخش اصلی است:

- `global`: سیاست سراسری
- `override`: تنظیم اختصاصی دوربین یا `null`
- `effective`: تنظیم نهایی قابل اعمال

## تغییرات پایگاه داده

مهاجرت Alembic زیر اضافه و روی پایگاه داده اجرا شد:

```text
20260815_0060_recording_settings.py
```

جدول‌های جدید:

- `recording_settings`: رکورد singleton تنظیمات سراسری
- `recording_camera_settings`: تنظیمات اختصاصی بر اساس `source_uri`

ستون‌های جدید در `recording_jobs`:

- `quality_preset`
- `retention_days`
- `output_width`
- `output_height`
- `output_fps`
- `output_bitrate_bps`

محدودیت‌های پایگاه داده فقط مقادیر تعریف‌شده برای کیفیت، مدت قطعه و مدت نگهداری را می‌پذیرند.

## تغییرات MinIO

### متادیتا و Tag

هنگام بارگذاری ویدئو، اطلاعات زیر در MinIO ثبت می‌شوند:

- شناسه دوربین
- زمان شروع و پایان
- کیفیت خروجی
- مدت نگهداری
- نام شیء
- اندازه و SHA-256 برای ضبط‌های زمان‌بندی‌شده

برای اجرای سیاست نگهداری، tag زیر روی اشیا قرار می‌گیرد:

```text
retention-days=7|30|90|365
```

### قوانین چرخه عمر

قانون عمومی ۳۰روزه با چهار قانون مبتنی بر tag جایگزین شد:

- `recording-retention-7`
- `recording-retention-30`
- `recording-retention-90`
- `recording-retention-365`

اشیای قدیمی فاقد tag در زمان راه‌اندازی بررسی می‌شوند و tag نگهداری ۳۰روزه دریافت می‌کنند. این عملیات محدود و قابل تکرار است.

### ایمنی فایل محلی

- فایل spool تا قبل از موفقیت و تأیید بارگذاری حذف نمی‌شود.
- در صورت خطای MinIO، فایل محلی برای تلاش مجدد باقی می‌ماند.
- فایل و sidecar فقط پس از بارگذاری موفق حذف می‌شوند.

## تغییرات فنی شاخه ویدئو

- سیاست مؤثر هر دوربین از `RecordingSettingsStore` دریافت می‌شود.
- شاخه عادی تمام‌صفحه دیگر `splitmuxsink` ضبط را ایجاد نمی‌کند.
- مالک پایدار ضبط پیوسته یا ضبط زمان‌بندی‌شده شاخه ذخیره را فعال می‌کند.
- `nvvideoconvert`، caps خروجی و `nvv4l2h264enc` مطابق preset تنظیم می‌شوند.
- قطعات MP4 توسط `splitmuxsink` ساخته و به صف غیرهمزمان MinIO تحویل داده می‌شوند.
- شاخه زنده و مسیر AI منتظر شبکه MinIO نمی‌مانند.

## استقرار انجام‌شده

- پکیج Python مربوط به MinIO در تصویر DeepStream فعال شد.
- تصویر `video-ai-router` بازسازی شد.
- مهاجرت `20260815_0060` اجرا شد.
- سرویس `video-ai-router` بازراه‌اندازی شد.
- ایجاد bucket و پیکربندی lifecycle بدون خطا انجام شد.
- bucket مورد استفاده همچنان `recordings` است.

## آزمون و اعتبارسنجی

- ۵۳ آزمون متمرکز ضبط، ذخیره‌سازی، هماهنگ‌کننده، تنظیمات، داشبورد و شاخه زنده با موفقیت اجرا شدند.
- فایل‌های Python با `py_compile` بررسی شدند.
- `git diff --check` خطای قالب‌بندی گزارش نکرد.
- API زنده با کاربر دارای مجوز بررسی شد و مقادیر پیش‌فرض و شناسه‌های امن دوربین را برگرداند.
- چهار قانون lifecycle در MinIO فعال و قابل مشاهده هستند.
- راه‌اندازی سرویس بدون خطای پیکربندی ضبط کامل شد.
- اجرای کل مجموعه آزمون‌ها دو بار آغاز شد، اما به‌ترتیب پس از ۲ و ۵ دقیقه به timeout رسید و قبل از timeout خطای assertion گزارش نشد.

## فایل‌های اصلی تغییرکرده

- `app/core/recording_settings_store.py`
- `app/api/recording_settings.py`
- `app/core/live_branch.py`
- `app/core/recording_storage.py`
- `app/core/recording_job_store.py`
- `app/core/recording_coordinator.py`
- `app/web/dashboard.html`
- `app/database.py`
- `alembic/versions/20260815_0060_recording_settings.py`
