---
description: Prompt-scoped alias for implementing selected previous-project options supplied through the Excel catalog
agent: realtime-orchestrator
---
Implement only the Excel-catalog apps, routes, options, or behaviors explicitly described by: $ARGUMENTS

There is no previous source repository. Use the current prompt, selected Excel fields, and verified current-project patterns. Keep unselected items `PENDING`, record assumptions, implement complete propagation for selected items, and run focused, integration, regression, deployment, and representative real-time checks.

Never claim behavioral parity without evidence. Never mark an item complete when it is unreachable, not consumed at runtime, underspecified in a critical way, or not evaluated honestly.
