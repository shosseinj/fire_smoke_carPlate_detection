---
description: Implement a feature end to end with inferred propagation, tests, regression checks, runtime validation, and knowledge updates
agent: realtime-orchestrator
---
Implement this requested behavior: $ARGUMENTS

Load the `change-propagation`, `project-evaluation`, and `real-time-validation` skills.

Before application-code edits, show a concise verified impact map, affected sections, acceptance tests, compatibility plan, and rollback approach. Then implement the smallest coherent end-to-end change.

The user does not need to list every required endpoint, schema, UI, configuration, worker, pipeline, storage, output, deployment, documentation, or test location. Infer them from the architecture.

Run focused tests, connected-section integration tests, relevant regression tests, and representative runtime/deployment validation. Use honest statuses and update verified agent knowledge.
